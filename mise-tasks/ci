#!/usr/bin/env nu
#MISE description="CI: parse-check + cli tests + gateway tests"
#MISE depends=["ci-parse-check", "test-cli", "test-gateway"]

# SSOT for what CI runs. Workflow file just calls `mise run ci` — to change
# what CI does, edit this depends list. Trimmed from the previous shape:
# - 4-build-cli dropped (`cargo test` in test-cli already compiles cli/)
# - test-ripgit dropped (upstream's ci.yml runs cargo test in ripgit on main
#   pushes; no value mirroring)

def main [] {
  print "✓ all CI checks passed"
}
