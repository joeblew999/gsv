#!/usr/bin/env nu
#MISE description="Full CI: parse-check + cli build/test + gateway tests + ripgit tests"
#MISE depends=["ci-parse-check", "4-build-cli", "test-cli", "test-gateway", "test-ripgit"]

# Mirrors what upstream's ci.yml validates that's actually
# cross-platform-meaningful — drops only the wrangler --dry-run bundle jobs
# (those are heavy and covered by upstream's own CI on main).

def main [] {
  print "✓ all CI checks passed"
}
