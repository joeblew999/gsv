#!/usr/bin/env nu
#MISE description="Run ripgit Rust unit tests (cargo test, native target — no wasm bundle)"

const SCRIPT_PATH = (path self)
const REPO_ROOT = ($SCRIPT_PATH | path dirname | path dirname)

def main [] {
  cd $"($REPO_ROOT)/ripgit"
  ^cargo test
}
