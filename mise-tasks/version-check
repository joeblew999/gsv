#!/usr/bin/env nu
#MISE description="Verify version consistency across the workspace (mirrors upstream's npm run version:check)"

const SCRIPT_PATH = (path self)
const REPO_ROOT = ($SCRIPT_PATH | path dirname | path dirname)

def main [] {
  cd $REPO_ROOT
  ^node ./scripts/version.mjs check
}
